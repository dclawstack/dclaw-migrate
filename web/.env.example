# URL of the deployed FastAPI backend.
# On Vercel: set this in Project Settings → Environment Variables.
# Locally: point at your running backend (Docker or uvicorn).
BACKEND_URL=http://localhost:8033
