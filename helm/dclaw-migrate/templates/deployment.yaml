apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dclaw-migrate.fullname" . }}
  labels:
    {{- include "dclaw-migrate.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "dclaw-migrate.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "dclaw-migrate.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "dclaw-migrate.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8121
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
