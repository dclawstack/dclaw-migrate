apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-migrate.fullname" . }}
  labels:
    {{- include "dclaw-migrate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-migrate.selectorLabels" . | nindent 4 }}
